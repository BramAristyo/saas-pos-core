info:
  name: Restore
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{url}}/bundling/{{bundling_id}}/restore"
  auth: inherit

examples:
  - name: success restore bundling
    request:
      url: "{{url}}/bundling/f4067b21-522b-5d3e-05e4-d4d3f7e7d4f2/restore"
      method: PATCH
    response:
      status: 200
