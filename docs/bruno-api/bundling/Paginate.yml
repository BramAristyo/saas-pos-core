info:
  name: Paginate
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/bundling"
  auth: inherit
  query:
    page: 1
    pageSize: 10

examples:
  - name: success paginate bundling
    request:
      url: "{{url}}/bundling?page=1&pageSize=10"
      method: GET
    response:
      status: 200
