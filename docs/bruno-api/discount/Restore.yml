info:
  name: Restore
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{url}}/discounts/{{discount_id}}/restore"
  auth: inherit

examples:
  - name: success restore discount
    request:
      url: "{{url}}/discounts/f4067b21-522b-5d3e-05e4-d4d3f7e7d4f2/restore"
      method: PATCH
    response:
      status: 200
