info:
  name: Update
  type: http
  seq: 4

http:
  method: PUT
  url: "{{url}}/taxes/{{tax_id}}"
  auth: inherit
  body:
    type: json
    data: |-
      {
        "name": "Updated Tax",
        "percentage": 12,
        "isActive": true
      }

examples:
  - name: success update tax
    request:
      url: "{{url}}/taxes/f4067b21-522b-5d3e-05e4-d4d3f7e7d4f2"
      method: PUT
      body:
        type: json
        data: |-
          {
            "name": "Updated Tax",
            "percentage": 12,
            "isActive": true
          }
    response:
      status: 200
      statusText: OK
