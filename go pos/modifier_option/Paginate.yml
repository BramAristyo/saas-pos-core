info:
  name: Paginate
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/modifier-options?page=1&pageSize=10"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
