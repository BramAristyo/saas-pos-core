info:
  name: Paginate
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/discounts"
  auth: inherit
  query:
    page: 1
    pageSize: 10

examples:
  - name: success paginate discount
    request:
      url: "{{url}}/discounts?page=1&pageSize=10"
      method: GET
    response:
      status: 200
