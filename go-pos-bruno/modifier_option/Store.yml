info:
  name: Store
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/modifier-options"
  auth: inherit
  body:
    type: json
    data: |-
      {
        "modifier_group_id": "00000000-0000-0000-0000-000000000000",
        "name": "Small",
        "price_adjustment": 500,
        "cogs_adjustment": 200
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
