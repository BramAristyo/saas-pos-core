info:
  name: Gross Profit
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/reports/gross-profit"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
