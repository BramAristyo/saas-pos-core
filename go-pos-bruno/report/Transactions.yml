info:
  name: Transactions
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/reports/transactions"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
