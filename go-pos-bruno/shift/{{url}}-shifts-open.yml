info:
  name: "{{url}}/shifts/open"
  type: http
  seq: 6

http:
  method: GET
  url: "{{url}}/shifts/open"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
