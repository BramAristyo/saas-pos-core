info:
  name: Find By Id
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/orders/{{orderId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
