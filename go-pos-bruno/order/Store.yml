info:
  name: Store
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/orders"
  auth: inherit
  body:
    type: json
    data: |-
      {
        "salesTypeId": "uuid",
        "taxId": "uuid",
        "discountId": "uuid",
        "items": [
          {
            "productId": "uuid",
            "quantity": 1
          }
        ],
        "payments": [
          {
            "method": "CASH",
            "amount": 100000
          }
        ]
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
