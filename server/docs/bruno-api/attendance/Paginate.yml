info:
  name: Paginate
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/attendances"
  auth: inherit

query:
  ~page: 1
  ~pageSize: 10
  ~search: 

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
