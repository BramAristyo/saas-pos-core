info:
  name: Delete
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{url}}/employees/:id"
  auth: inherit

vars:
  pre-request:
    id: 00000000-0000-0000-0000-000000000801

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: example
    request:
      url: "{{url}}/employees/00000000-0000-0000-0000-000000000801"
      method: DELETE
    response:
      status: 200
      statusText: OK
      headers:
        - name: content-type
          value: application/json; charset=utf-8
      body:
        type: json
        data: |-
          {
            "success": true,
            "message": "deleted successfully"
          }
