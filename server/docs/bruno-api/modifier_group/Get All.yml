info:
  name: Get All
  type: http
  seq: 7

http:
  method: GET
  url: "{{url}}/modifier-groups/get-all"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
