info:
  name: Gracefull Test
  type: http
  seq: 15

http:
  method: GET
  url: "{{naked_url}}/slow"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
