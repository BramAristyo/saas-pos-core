info:
  name: Me
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/me"

runtime:
  variables:
    - name: url
      value: http://localhost:9000/api/v1

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
