info:
  name: Delete
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{url}}/bundling/{{bundling_id}}"
  auth: inherit

examples:
  - name: success delete bundling
    request:
      url: "{{url}}/bundling/00000000-0000-0000-0000-000000000602"
      method: DELETE
    response:
      status: 200
