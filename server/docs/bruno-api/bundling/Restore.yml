info:
  name: Restore
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{url}}/bundling/{{bundling_id}}/restore"
  auth: inherit

examples:
  - name: success restore bundling
    request:
      url: "{{url}}/bundling/00000000-0000-0000-0000-000000000602/restore"
      method: PATCH
    response:
      status: 200
