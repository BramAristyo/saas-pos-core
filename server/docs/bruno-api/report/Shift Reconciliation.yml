info:
  name: Shift Reconciliation
  type: http
  seq: 5

http:
  method: GET
  url: "{{url}}/reports/shift-reconciliation"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
